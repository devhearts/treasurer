services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    # Persist SQLite DB if your app uses ./data or DATABASE_URL path
    volumes:
      - app-data:/app/data
    restart: unless-stopped

volumes:
  app-data:
