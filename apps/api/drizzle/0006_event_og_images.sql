-- Data migration: run `npm run db:backfill-event-og -w @treasurer/api` once per environment
-- after deploying the OG upload code. Generates events/{eventId}/og.png from slot-0 gallery images.
SELECT 1;
